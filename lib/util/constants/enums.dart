// LISTS OF ENUMS
// The Enums cannot be created inside a class

enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered }

enum PaymentMethods { googlePay, visa, mastercard, verve, monnify, razorPay, paytm, paystack }